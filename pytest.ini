[pytest]
testpaths = tests
markers =
    network: kräver live-åtkomst till en extern tjänst (Riksdagens API).
        Körs separat i CI så att en störning hos tredje part inte
        blockerar en merge.
